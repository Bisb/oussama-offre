version: '3.8'

services:
  # Frontend service
  frontend:
    build:
      context: .
    ports:
      - "3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    volume:
      - ./:/app
    restart: unless-stopped